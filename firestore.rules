rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permitir lectura pública de los datos del portfolio
      allow read: if true;
      // Solo permitir escritura al administrador autenticado
      allow write: if request.auth != null;
    }
  }
}
